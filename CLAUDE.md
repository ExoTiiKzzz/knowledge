# Géo Quiz

## Agent skills

### Issue tracker

Les issues vivent en markdown local, sous `.scratch/<feature>/`. See `docs/agents/issue-tracker.md`.

### Triage labels

Les cinq rôles canoniques, chaque label portant son propre nom. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context — un `CONTEXT.md` et `docs/adr/` à la racine. See `docs/agents/domain.md`.
